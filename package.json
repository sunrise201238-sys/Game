{
  "name": "slingshot-strategy-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "bootstrap": "npm ci --workspaces",
    "build": "npm run --workspace @slingshot/shared build && npm run --workspace @slingshot/server build && npm run --workspace @slingshot/client build",
    "start": "npm run --workspace @slingshot/server start",
    "start:server": "npm run --workspace @slingshot/server start",
    "lint": "npm run lint --workspace=@slingshot/client && npm run lint --workspace=@slingshot/server",
    "format": "npm run format --workspace=@slingshot/client && npm run format --workspace=@slingshot/server"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=9"
  },
  "packageManager": "npm@10"
}
