{
  "name": "slingshot-strategy-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "bootstrap": "npm ci --workspaces",
    "build": "npm run build --workspace @slingshot/shared && npm run build --workspace @slingshot/server && npm run build --workspace @slingshot/client",
    "start": "npm run start --workspace @slingshot/server",
    "start:server": "npm run start --workspace @slingshot/server",
    "lint": "npm run lint --workspace @slingshot/client && npm run lint --workspace @slingshot/server",
    "format": "npm run format --workspace @slingshot/client && npm run format --workspace @slingshot/server"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=9"
  },
  "packageManager": "npm@10"
}
